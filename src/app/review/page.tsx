// const [media, setMedia] = useState<File[]>([]);

// type InputChange = typeof ChangeEvent<HTMLInputElement>;

// const handleChange = (e: InputChange) => {
//     const target = e.target as HTMLInputElement;
//     const files = [...Object.values(target.files)];
//     setMedia([...files]);
// };
